asyncapi: '2.5.0'
info:
  title: 👥 Team API
  version: '0.0.1'
  termsOfService: http://www.someURL/terms
  contact:
    name: API Support
    url: https://www.help.me
    email: support@example.com
  description: |
    Allows you to interact and find information about teams including their focus and fundamental team type

    ### features:

    * 📊 Team Metrics
    * 🗣️ How to get in touch
    * 📚 Documentation
    
  license:
    name: MIT
    url: https://opensource.org/license/mit/

servers:
  production:
    url: https://bagg3rs.github.io/
    protocol: https
    description: HTTPS broker

channels:
  user/signedup:
    subscribe:
      operationId: emitUserSignUpEvent
      message:
        $ref : '#/components/messages/UserSignedUp'
components:
  messages:
    UserSignedUp:
      name: userSignedUp
      title: User signed up event
      summary: Inform about a new user registration in the system
      contentType: application/json
      payload:
        $ref: '#/components/schemas/userSignedUpPayload'

  schemas:
    userSignedUpPayload:
      type: object
      properties:
        firstName:
          type: string
          description: "foo"
        lastName:
          type: string
          description: "bar"
        email:
          type: string
          format: email
          description: "baz"
        createdAt:
          type: string
          format: date-time 
